# ─── LLM providers (optional — agents use rule-based logic if absent) ────────
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# ─── Connector APIs (optional — stubs return realistic demo data if absent) ──
EXPERIAN_API_KEY=
EQUIFAX_API_KEY=
DNB_API_KEY=
AML_KYC_API_KEY=
BLOOMBERG_API_KEY=

# ─── Database / checkpointer ──────────────────────────────────────────────────
# Local dev: leave blank → uses SQLite (acos.db in backend/)
DATABASE_URL=

# Production (Netlify Database / Supabase PostgreSQL):
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# ─── Server ───────────────────────────────────────────────────────────────────
PORT=8000
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:4173
