# Dependencies
app/node_modules/
node_modules/

# Build output
app/dist/
dist/

# Netlify
.netlify/

# OS / editor
.DS_Store
Thumbs.db
*.local

# Large binary board decks (keep in OneDrive/SharePoint, not git)
*.pptx
*.docx

# Cursor internal
.cursor/

# Claude Code local config (absolute paths / machine-specific)
.claude/

# Bundled portable Node toolchain (do not commit — ~100MB)
.tools/

# Index exports (regenerated by scripts/reindex.py)
_index/

# Playwright
app/test-results/

# Python backend
backend/.venv/
backend/__pycache__/
backend/**/__pycache__/
backend/*.db
backend/*.db-shm
backend/*.db-wal

# Environment files (contain machine/deploy-specific config, e.g. DB credentials)
.env
app/.env
backend/.env

